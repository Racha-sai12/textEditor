@inherits LayoutComponentBase

<div class="page">
    <main>
     @Body
    </main>
</div>
@* <style>
    .parent {
        height: 400px; /* Set a specific height for the parent */
        border: 1px solid #ccc; /* Optional: Border for visual reference */
        overflow: hidden; /* Prevent overflow on parent */
    }

    .scrollable {
        height: 100%; /* Stretch to parent height */
        overflow-y: auto; /* Enable vertical scrolling */
        background-color: #f8f9fa; /* Optional: Background color */
    }
</style> *@